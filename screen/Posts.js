import { Button, StyleSheet, Text, View, CheckBox } from "react-native";
import React from "react";

const Posts = () => {
  const [isSelected, setSelection] = useState(false);

  return <View></View>;
};

export default Posts;
